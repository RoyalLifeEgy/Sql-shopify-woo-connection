# Main Application Dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0.25
alembic>=1.13.0

# Security
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
python-multipart>=0.0.6
cryptography>=42.0.0

# Task Scheduling
apscheduler>=3.10.0

# E-commerce Integrations
requests>=2.31.0
httpx>=0.26.0

# Utilities
python-dotenv>=1.0.0
tenacity>=8.2.0

# Optional Database Drivers (install only what you need)
# For PostgreSQL: pip install psycopg2-binary (or use requirements-postgres.txt)
# For MySQL: pip install pymysql (or use requirements-mysql.txt)
# For MS SQL: pip install pyodbc (or use requirements-mssql.txt)
# SQLite is included with Python by default
